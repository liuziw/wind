<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyzs.tibmas.core.mapper.UserSecretQuestionMapper">

    <!--
    /**
     * 代码自动生成，请不要修改，切记！！！
     */
    -->

    <sql id="selectAllPropertiesAsDO">
            select  id id, user_account userAccount, question_id questionId, question_answer questionAnswer, seq seq, gmt_create gmtCreate, gmt_modified gmtModified, is_deleted isDeleted, user_id_create userIdCreate, user_id_modified userIdModified from user_secret_question
    </sql>

    <sql id="selectByFieldNamesAsDO">
       select <foreach collection="fieldNames" index="index" item="item"  separator=",">${item}</foreach> from user_secret_question
    </sql>

    <sql id="selectAsDO">
        <choose>
            <when test="fieldNames==null or fieldNames.length==0">
                  <include refid="selectAllPropertiesAsDO"/>
            </when>
            <otherwise>
                  <include refid="selectByFieldNamesAsDO"/>
            </otherwise>
        </choose>
    </sql>


    <select id="listAll" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>

    <select id="mapAll" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>


    <select id="getById" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" /> where id=#{id} and is_deleted=0
    </select>

    <select id="listByIds" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <select id="mapByIds" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <update id="deleteById">
        update  user_secret_question set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id=#{id}
    </update>

    <update id="deleteByIds">
        update  user_secret_question set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </update>


    <update id="updateNotNullProperties">
        update user_secret_question set
                <if test="userAccount != null">user_account=#{userAccount},</if>
                <if test="questionId != null">question_id=#{questionId},</if>
                <if test="questionAnswer != null">question_answer=#{questionAnswer},</if>
                <if test="seq != null">seq=#{seq},</if>
                <if test="userIdCreate != null">user_id_create=#{userIdCreate},</if>
                <if test="userIdModified != null">user_id_modified=#{userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>

    <update id="updateAllProperties">
        update user_secret_question set
                user_account=#{userAccount},
                question_id=#{questionId},
                question_answer=#{questionAnswer},
                seq=#{seq},
                user_id_create=#{userIdCreate},
                user_id_modified=#{userIdModified},
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>


    <insert id="insertAllProperties">
              <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                        SELECT LAST_INSERT_ID() as id
                      </selectKey>

        insert into user_secret_question
        (
                user_account,
                question_id,
                question_answer,
                seq,
                user_id_create,
                user_id_modified,
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                #{userAccount},
                #{questionId},
                #{questionAnswer},
                #{seq},
                #{userIdCreate},
                #{userIdModified},
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>


    <insert id="insertNotNullProperties">
                      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                                SELECT LAST_INSERT_ID() as id
                              </selectKey>
        insert into user_secret_question
        (
                <if test="userAccount!=null">user_account,</if>
                <if test="questionId!=null">question_id,</if>
                <if test="questionAnswer!=null">question_answer,</if>
                <if test="seq!=null">seq,</if>
                <if test="userIdCreate!=null">user_id_create,</if>
                <if test="userIdModified!=null">user_id_modified,</if>
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                <if test="userAccount!=null">#{userAccount},</if>
                <if test="questionId!=null">#{questionId},</if>
                <if test="questionAnswer!=null">#{questionAnswer},</if>
                <if test="seq!=null">#{seq},</if>
                <if test="userIdCreate!=null">#{userIdCreate},</if>
                <if test="userIdModified!=null">#{userIdModified},</if>
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>

        <insert id="insertAllPropertiesList">
           insert into user_secret_question
           (
                   user_account,
                   question_id,
                   question_answer,
                   seq,
                   user_id_create,
                   user_id_modified,
                   is_deleted,gmt_create,gmt_modified
                   )
           values
           <foreach collection="list" item="item" index="index" separator="," >
           (
                   #{item.userAccount},
                   #{item.questionId},
                   #{item.questionAnswer},
                   #{item.seq},
                   #{item.userIdCreate},
                   #{item.userIdModified},
                   0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
           )
           </foreach>
       </insert>



    <select id="listByProperty" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="countByProperty" resultType="java.lang.Integer">
        select count(1) from user_secret_question
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="mapByProperty" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>


    <update id="updateNotNullFieldsByProperty">
        update user_secret_question set
                <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="updateAllFieldsByProperty">
        update user_secret_question set
                user_account=#{updateDO.userAccount},
                question_id=#{updateDO.questionId},
                question_answer=#{updateDO.questionAnswer},
                seq=#{updateDO.seq},
                user_id_create=#{updateDO.userIdCreate},
                user_id_modified=#{updateDO.userIdModified},
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="deleteByProperty">
        update user_secret_question set
                is_delete=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <select id="listByProperties" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="countByProperties" resultType="java.lang.Integer">
        select count(1) from user_secret_question
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="mapByProperties" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <update id="updateNotNullFieldsByProperties">
        update user_secret_question set
                <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="updateAllFieldsByProperties">
        update user_secret_question set
                user_account=#{updateDO.userAccount}
                question_id=#{updateDO.questionId}
                question_answer=#{updateDO.questionAnswer}
                seq=#{updateDO.seq}
                user_id_create=#{updateDO.userIdCreate}
                user_id_modified=#{updateDO.userIdModified}
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="deleteByProperties">
        update user_secret_question set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <select id="_listByQueryItems" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_mapByQueryItems" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_countByQueryItems" resultType="java.lang.Integer">
        select count(1) from user_secret_question
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

   <update id="_updateNotNullFieldsByQueryItems">
         update user_secret_question set
                <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_updateNotAllFieldsByQueryItems">
         update user_secret_question set
                user_account=#{updateDO.userAccount}
                question_id=#{updateDO.questionId}
                question_answer=#{updateDO.questionAnswer}
                seq=#{updateDO.seq}
                user_id_create=#{updateDO.userIdCreate}
                user_id_modified=#{updateDO.userIdModified}
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_deleteByQueryItems">
         update user_secret_question set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <select id="_listByPageQuery" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                         #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
            <if test="pageQuery.sortFieldNameForDatabase!=null">
                order by ${pageQuery.sortFieldNameForDatabase} ${pageQuery.sortType}
            </if>
            limit #{values.pageOffset},#{pageQuery.pageSize}
    </select>


    <select id="_countByPageQuery" resultType="java.lang.Integer">
        select count(1) from user_secret_question
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                    <choose>
                        <when test="item.type.optr=='in' || item.type.optr=='not in'">
                            <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                                #{inItem}
                            </foreach>
                        </when>
                        <otherwise>
                           #{values.[${item.valueKeyName}]}
                        </otherwise>
                    </choose>
                </if>
            </foreach>
    </select>



          <select id="listByUserAccount" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_account=#{userAccount}
          </select>

          <select id="countByUserAccount" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_account=#{userAccount}
           </select>

           <select id="mapByUserAccount" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and user_account=#{userAccount}
           </select>

           <update id="updateNotNullFieldsByUserAccount">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and user_account=#{userAccount}
          </update>

          <update id="updateAllFieldsByUserAccount">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_account=#{userAccount}
            </update>

            <update id="deleteByUserAccount">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_account=#{userAccount}
            </update>



            <!--property in -->

         <select id="listByInUserAccount" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_account in
                  <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInUserAccount" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_account in
                      <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInUserAccount" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and user_account in
                        <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInUserAccount">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_account in
                       <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInUserAccount">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_account in
                       <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInUserAccount">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and user_account in
                        <foreach collection="userAccountList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByQuestionId" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and question_id=#{questionId}
          </select>

          <select id="countByQuestionId" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and question_id=#{questionId}
           </select>

           <select id="mapByQuestionId" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and question_id=#{questionId}
           </select>

           <update id="updateNotNullFieldsByQuestionId">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and question_id=#{questionId}
          </update>

          <update id="updateAllFieldsByQuestionId">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and question_id=#{questionId}
            </update>

            <update id="deleteByQuestionId">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and question_id=#{questionId}
            </update>



            <!--property in -->

         <select id="listByInQuestionId" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and question_id in
                  <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInQuestionId" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and question_id in
                      <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInQuestionId" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and question_id in
                        <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInQuestionId">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and question_id in
                       <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInQuestionId">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and question_id in
                       <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInQuestionId">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and question_id in
                        <foreach collection="questionIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByQuestionAnswer" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and question_answer=#{questionAnswer}
          </select>

          <select id="countByQuestionAnswer" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and question_answer=#{questionAnswer}
           </select>

           <select id="mapByQuestionAnswer" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and question_answer=#{questionAnswer}
           </select>

           <update id="updateNotNullFieldsByQuestionAnswer">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and question_answer=#{questionAnswer}
          </update>

          <update id="updateAllFieldsByQuestionAnswer">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and question_answer=#{questionAnswer}
            </update>

            <update id="deleteByQuestionAnswer">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and question_answer=#{questionAnswer}
            </update>



            <!--property in -->

         <select id="listByInQuestionAnswer" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and question_answer in
                  <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInQuestionAnswer" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and question_answer in
                      <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInQuestionAnswer" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and question_answer in
                        <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInQuestionAnswer">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and question_answer in
                       <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInQuestionAnswer">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and question_answer in
                       <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInQuestionAnswer">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and question_answer in
                        <foreach collection="questionAnswerList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listBySeq" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and seq=#{seq}
          </select>

          <select id="countBySeq" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and seq=#{seq}
           </select>

           <select id="mapBySeq" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and seq=#{seq}
           </select>

           <update id="updateNotNullFieldsBySeq">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and seq=#{seq}
          </update>

          <update id="updateAllFieldsBySeq">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and seq=#{seq}
            </update>

            <update id="deleteBySeq">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and seq=#{seq}
            </update>



            <!--property in -->

         <select id="listByInSeq" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and seq in
                  <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInSeq" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and seq in
                      <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInSeq" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and seq in
                        <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInSeq">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and seq in
                       <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInSeq">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and seq in
                       <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInSeq">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and seq in
                        <foreach collection="seqList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByUserIdCreate" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create=#{userIdCreate}
          </select>

          <select id="countByUserIdCreate" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_id_create=#{userIdCreate}
           </select>

           <select id="mapByUserIdCreate" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create=#{userIdCreate}
           </select>

           <update id="updateNotNullFieldsByUserIdCreate">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and user_id_create=#{userIdCreate}
          </update>

          <update id="updateAllFieldsByUserIdCreate">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_create=#{userIdCreate}
            </update>

            <update id="deleteByUserIdCreate">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_create=#{userIdCreate}
            </update>



            <!--property in -->

         <select id="listByInUserIdCreate" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create in
                  <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInUserIdCreate" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_id_create in
                      <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInUserIdCreate" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and user_id_create in
                        <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInUserIdCreate">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_create in
                       <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInUserIdCreate">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_create in
                       <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInUserIdCreate">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and user_id_create in
                        <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByUserIdModified" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified=#{userIdModified}
          </select>

          <select id="countByUserIdModified" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_id_modified=#{userIdModified}
           </select>

           <select id="mapByUserIdModified" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified=#{userIdModified}
           </select>

           <update id="updateNotNullFieldsByUserIdModified">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and user_id_modified=#{userIdModified}
          </update>

          <update id="updateAllFieldsByUserIdModified">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_modified=#{userIdModified}
            </update>

            <update id="deleteByUserIdModified">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_modified=#{userIdModified}
            </update>



            <!--property in -->

         <select id="listByInUserIdModified" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified in
                  <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInUserIdModified" resultType="java.lang.Integer">
                select count(1) from user_secret_question
                where is_deleted=0 and user_id_modified in
                      <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInUserIdModified" resultType="com.hyzs.tibmas.core.dbo.UserSecretQuestionDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and user_id_modified in
                        <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInUserIdModified">
              update user_secret_question set
                      <if test="updateDO.userAccount != null">user_account=#{updateDO.userAccount},</if>
                      <if test="updateDO.questionId != null">question_id=#{updateDO.questionId},</if>
                      <if test="updateDO.questionAnswer != null">question_answer=#{updateDO.questionAnswer},</if>
                      <if test="updateDO.seq != null">seq=#{updateDO.seq},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_modified in
                       <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInUserIdModified">
                update user_secret_question set
                        user_account=#{updateDO.userAccount},
                        question_id=#{updateDO.questionId},
                        question_answer=#{updateDO.questionAnswer},
                        seq=#{updateDO.seq},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_modified in
                       <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInUserIdModified">
                update user_secret_question set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and user_id_modified in
                        <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>

</mapper>

