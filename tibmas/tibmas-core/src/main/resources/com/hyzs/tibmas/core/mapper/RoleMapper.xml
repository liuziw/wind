<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzw.wind.tibmas.core.mapper.RoleMapper">

    <!--
    /**
     * 代码自动生成，请不要修改，切记！！！
     */
    -->

    <sql id="selectAllPropertiesAsDO">
            select  id id, role_code roleCode, role_name roleName, role_desc roleDesc, role_type roleType, app_id appId, is_extend isExtend, is_deleted isDeleted, gmt_create gmtCreate, gmt_modified gmtModified, user_id_create userIdCreate, user_id_modified userIdModified from role
    </sql>

    <sql id="selectByFieldNamesAsDO">
       select <foreach collection="fieldNames" index="index" item="item"  separator=",">${item}</foreach> from role
    </sql>

    <sql id="selectAsDO">
        <choose>
            <when test="fieldNames==null or fieldNames.length==0">
                  <include refid="selectAllPropertiesAsDO"/>
            </when>
            <otherwise>
                  <include refid="selectByFieldNamesAsDO"/>
            </otherwise>
        </choose>
    </sql>


    <select id="listAll" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>

    <select id="mapAll" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>


    <select id="getById" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" /> where id=#{id} and is_deleted=0
    </select>


    <select id="listByIds" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <select id="mapByIds" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <update id="deleteById">
        update  role set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id=#{id}
    </update>

    <update id="deleteByIds">
        update  role set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </update>


    <update id="updateNotNullProperties">
        update role set
                <if test="id != null">id=#{id},</if>
                <if test="roleCode != null">role_code=#{roleCode},</if>
                <if test="roleName != null">role_name=#{roleName},</if>
                <if test="roleDesc != null">role_desc=#{roleDesc},</if>
                <if test="roleType != null">role_type=#{roleType},</if>
                <if test="appId != null">app_id=#{appId},</if>
                <if test="isExtend != null">is_extend=#{isExtend},</if>
                <if test="userIdCreate != null">user_id_create=#{userIdCreate},</if>
                <if test="userIdModified != null">user_id_modified=#{userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>

    <update id="updateAllProperties">
        update role set
                id=#{id},
                role_code=#{roleCode},
                role_name=#{roleName},
                role_desc=#{roleDesc},
                role_type=#{roleType},
                app_id=#{appId},
                is_extend=#{isExtend},
                user_id_create=#{userIdCreate},
                user_id_modified=#{userIdModified},
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>


    <insert id="insertAllProperties">
              <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                        SELECT LAST_INSERT_ID() as id
                      </selectKey>

        insert into role
        (
                id,
                role_code,
                role_name,
                role_desc,
                role_type,
                app_id,
                is_extend,
                user_id_create,
                user_id_modified,
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                #{id},
                #{roleCode},
                #{roleName},
                #{roleDesc},
                #{roleType},
                #{appId},
                #{isExtend},
                #{userIdCreate},
                #{userIdModified},
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>


    <insert id="insertNotNullProperties">
                      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                                SELECT LAST_INSERT_ID() as id
                              </selectKey>
        insert into role
        (
                <if test="id!=null">id,</if>
                <if test="roleCode!=null">role_code,</if>
                <if test="roleName!=null">role_name,</if>
                <if test="roleDesc!=null">role_desc,</if>
                <if test="roleType!=null">role_type,</if>
                <if test="appId!=null">app_id,</if>
                <if test="isExtend!=null">is_extend,</if>
                <if test="userIdCreate!=null">user_id_create,</if>
                <if test="userIdModified!=null">user_id_modified,</if>
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                <if test="id!=null">#{id},</if>
                <if test="roleCode!=null">#{roleCode},</if>
                <if test="roleName!=null">#{roleName},</if>
                <if test="roleDesc!=null">#{roleDesc},</if>
                <if test="roleType!=null">#{roleType},</if>
                <if test="appId!=null">#{appId},</if>
                <if test="isExtend!=null">#{isExtend},</if>
                <if test="userIdCreate!=null">#{userIdCreate},</if>
                <if test="userIdModified!=null">#{userIdModified},</if>
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>

        <insert id="insertAllPropertiesList">
           insert into role
           (
                   id,
                   role_code,
                   role_name,
                   role_desc,
                   role_type,
                   app_id,
                   is_extend,
                   user_id_create,
                   user_id_modified,
                   is_deleted,gmt_create,gmt_modified
                   )
           values
           <foreach collection="list" item="item" index="index" separator="," >
           (
                   #{item.id},
                   #{item.roleCode},
                   #{item.roleName},
                   #{item.roleDesc},
                   #{item.roleType},
                   #{item.appId},
                   #{item.isExtend},
                   #{item.userIdCreate},
                   #{item.userIdModified},
                   0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
           )
           </foreach>
       </insert>



    <select id="listByProperty" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="countByProperty" resultType="java.lang.Integer">
        select count(1) from role
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="mapByProperty" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>


    <update id="updateNotNullFieldsByProperty">
        update role set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="updateAllFieldsByProperty">
        update role set
                id=#{updateDO.id},
                role_code=#{updateDO.roleCode},
                role_name=#{updateDO.roleName},
                role_desc=#{updateDO.roleDesc},
                role_type=#{updateDO.roleType},
                app_id=#{updateDO.appId},
                is_extend=#{updateDO.isExtend},
                user_id_create=#{updateDO.userIdCreate},
                user_id_modified=#{updateDO.userIdModified},
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="deleteByProperty">
        update role set
                is_delete=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <select id="listByProperties" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="countByProperties" resultType="java.lang.Integer">
        select count(1) from role
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="mapByProperties" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <update id="updateNotNullFieldsByProperties">
        update role set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="updateAllFieldsByProperties">
        update role set
                id=#{updateDO.id}
                role_code=#{updateDO.roleCode}
                role_name=#{updateDO.roleName}
                role_desc=#{updateDO.roleDesc}
                role_type=#{updateDO.roleType}
                app_id=#{updateDO.appId}
                is_extend=#{updateDO.isExtend}
                user_id_create=#{updateDO.userIdCreate}
                user_id_modified=#{updateDO.userIdModified}
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="deleteByProperties">
        update role set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <select id="_listByQueryItems" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_mapByQueryItems" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_countByQueryItems" resultType="java.lang.Integer">
        select count(1) from role
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

   <update id="_updateNotNullFieldsByQueryItems">
         update role set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_updateNotAllFieldsByQueryItems">
         update role set
                id=#{updateDO.id}
                role_code=#{updateDO.roleCode}
                role_name=#{updateDO.roleName}
                role_desc=#{updateDO.roleDesc}
                role_type=#{updateDO.roleType}
                app_id=#{updateDO.appId}
                is_extend=#{updateDO.isExtend}
                user_id_create=#{updateDO.userIdCreate}
                user_id_modified=#{updateDO.userIdModified}
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_deleteByQueryItems">
         update role set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <select id="_listByPageQuery" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                         #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
            <if test="pageQuery.sortFieldNameForDatabase!=null">
                order by ${pageQuery.sortFieldNameForDatabase} ${pageQuery.sortType}
            </if>
            limit #{values.pageOffset},#{pageQuery.pageSize}
    </select>


    <select id="_countByPageQuery" resultType="java.lang.Integer">
        select count(1) from role
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                    <choose>
                        <when test="item.type.optr=='in' || item.type.optr=='not in'">
                            <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                                #{inItem}
                            </foreach>
                        </when>
                        <otherwise>
                           #{values.[${item.valueKeyName}]}
                        </otherwise>
                    </choose>
                </if>
            </foreach>
    </select>



          <select id="listById" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and id=#{id}
          </select>

          <select id="countById" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and id=#{id}
           </select>

           <select id="mapById" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and id=#{id}
           </select>

           <update id="updateNotNullFieldsById">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and id=#{id}
          </update>

          <update id="updateAllFieldsById">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and id=#{id}
            </update>




            <!--property in -->

         <select id="listByInId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and id in
                  <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInId" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and id in
                      <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and id in
                        <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInId">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and id in
                       <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInId">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and id in
                       <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInId">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and id in
                        <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByRoleCode" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_code=#{roleCode}
          </select>

          <select id="countByRoleCode" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_code=#{roleCode}
           </select>

           <select id="mapByRoleCode" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and role_code=#{roleCode}
           </select>

           <update id="updateNotNullFieldsByRoleCode">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and role_code=#{roleCode}
          </update>

          <update id="updateAllFieldsByRoleCode">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_code=#{roleCode}
            </update>


            <update id="deleteByRoleCode">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_code=#{roleCode}
            </update>



            <!--property in -->

         <select id="listByInRoleCode" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_code in
                  <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInRoleCode" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_code in
                      <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInRoleCode" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and role_code in
                        <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInRoleCode">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_code in
                       <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInRoleCode">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_code in
                       <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInRoleCode">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and role_code in
                        <foreach collection="roleCodeList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByRoleName" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_name=#{roleName}
          </select>

          <select id="countByRoleName" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_name=#{roleName}
           </select>

           <select id="mapByRoleName" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and role_name=#{roleName}
           </select>

           <update id="updateNotNullFieldsByRoleName">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and role_name=#{roleName}
          </update>

          <update id="updateAllFieldsByRoleName">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_name=#{roleName}
            </update>


            <update id="deleteByRoleName">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_name=#{roleName}
            </update>



            <!--property in -->

         <select id="listByInRoleName" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_name in
                  <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInRoleName" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_name in
                      <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInRoleName" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and role_name in
                        <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInRoleName">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_name in
                       <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInRoleName">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_name in
                       <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInRoleName">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and role_name in
                        <foreach collection="roleNameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByRoleDesc" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_desc=#{roleDesc}
          </select>

          <select id="countByRoleDesc" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_desc=#{roleDesc}
           </select>

           <select id="mapByRoleDesc" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and role_desc=#{roleDesc}
           </select>

           <update id="updateNotNullFieldsByRoleDesc">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and role_desc=#{roleDesc}
          </update>

          <update id="updateAllFieldsByRoleDesc">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_desc=#{roleDesc}
            </update>


            <update id="deleteByRoleDesc">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_desc=#{roleDesc}
            </update>



            <!--property in -->

         <select id="listByInRoleDesc" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_desc in
                  <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInRoleDesc" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_desc in
                      <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInRoleDesc" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and role_desc in
                        <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInRoleDesc">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_desc in
                       <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInRoleDesc">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_desc in
                       <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInRoleDesc">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and role_desc in
                        <foreach collection="roleDescList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByRoleType" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_type=#{roleType}
          </select>

          <select id="countByRoleType" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_type=#{roleType}
           </select>

           <select id="mapByRoleType" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and role_type=#{roleType}
           </select>

           <update id="updateNotNullFieldsByRoleType">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and role_type=#{roleType}
          </update>

          <update id="updateAllFieldsByRoleType">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_type=#{roleType}
            </update>


            <update id="deleteByRoleType">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and role_type=#{roleType}
            </update>



            <!--property in -->

         <select id="listByInRoleType" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and role_type in
                  <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInRoleType" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and role_type in
                      <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInRoleType" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and role_type in
                        <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInRoleType">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_type in
                       <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInRoleType">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and role_type in
                       <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInRoleType">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and role_type in
                        <foreach collection="roleTypeList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByAppId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and app_id=#{appId}
          </select>

          <select id="countByAppId" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and app_id=#{appId}
           </select>

           <select id="mapByAppId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and app_id=#{appId}
           </select>

           <update id="updateNotNullFieldsByAppId">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and app_id=#{appId}
          </update>

          <update id="updateAllFieldsByAppId">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and app_id=#{appId}
            </update>


            <update id="deleteByAppId">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and app_id=#{appId}
            </update>



            <!--property in -->

         <select id="listByInAppId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and app_id in
                  <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInAppId" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and app_id in
                      <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInAppId" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and app_id in
                        <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInAppId">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and app_id in
                       <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInAppId">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and app_id in
                       <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInAppId">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and app_id in
                        <foreach collection="appIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByIsExtend" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and is_extend=#{isExtend}
          </select>

          <select id="countByIsExtend" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and is_extend=#{isExtend}
           </select>

           <select id="mapByIsExtend" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and is_extend=#{isExtend}
           </select>

           <update id="updateNotNullFieldsByIsExtend">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and is_extend=#{isExtend}
          </update>

          <update id="updateAllFieldsByIsExtend">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and is_extend=#{isExtend}
            </update>


            <update id="deleteByIsExtend">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and is_extend=#{isExtend}
            </update>



            <!--property in -->

         <select id="listByInIsExtend" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and is_extend in
                  <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInIsExtend" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and is_extend in
                      <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInIsExtend" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and is_extend in
                        <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInIsExtend">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and is_extend in
                       <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInIsExtend">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and is_extend in
                       <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInIsExtend">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and is_extend in
                        <foreach collection="isExtendList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByUserIdCreate" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create=#{userIdCreate}
          </select>

          <select id="countByUserIdCreate" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and user_id_create=#{userIdCreate}
           </select>

           <select id="mapByUserIdCreate" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create=#{userIdCreate}
           </select>

           <update id="updateNotNullFieldsByUserIdCreate">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and user_id_create=#{userIdCreate}
          </update>

          <update id="updateAllFieldsByUserIdCreate">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_create=#{userIdCreate}
            </update>


            <update id="deleteByUserIdCreate">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_create=#{userIdCreate}
            </update>



            <!--property in -->

         <select id="listByInUserIdCreate" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_create in
                  <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInUserIdCreate" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and user_id_create in
                      <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInUserIdCreate" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and user_id_create in
                        <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInUserIdCreate">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_create in
                       <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInUserIdCreate">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_create in
                       <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInUserIdCreate">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and user_id_create in
                        <foreach collection="userIdCreateList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByUserIdModified" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified=#{userIdModified}
          </select>

          <select id="countByUserIdModified" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and user_id_modified=#{userIdModified}
           </select>

           <select id="mapByUserIdModified" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified=#{userIdModified}
           </select>

           <update id="updateNotNullFieldsByUserIdModified">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and user_id_modified=#{userIdModified}
          </update>

          <update id="updateAllFieldsByUserIdModified">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_modified=#{userIdModified}
            </update>


            <update id="deleteByUserIdModified">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and user_id_modified=#{userIdModified}
            </update>



            <!--property in -->

         <select id="listByInUserIdModified" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and user_id_modified in
                  <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInUserIdModified" resultType="java.lang.Integer">
                select count(1) from role
                where is_deleted=0 and user_id_modified in
                      <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInUserIdModified" resultType="com.lzw.wind.tibmas.core.dbo.RoleDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and user_id_modified in
                        <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInUserIdModified">
              update role set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.roleCode != null">role_code=#{updateDO.roleCode},</if>
                      <if test="updateDO.roleName != null">role_name=#{updateDO.roleName},</if>
                      <if test="updateDO.roleDesc != null">role_desc=#{updateDO.roleDesc},</if>
                      <if test="updateDO.roleType != null">role_type=#{updateDO.roleType},</if>
                      <if test="updateDO.appId != null">app_id=#{updateDO.appId},</if>
                      <if test="updateDO.isExtend != null">is_extend=#{updateDO.isExtend},</if>
                      <if test="updateDO.userIdCreate != null">user_id_create=#{updateDO.userIdCreate},</if>
                      <if test="updateDO.userIdModified != null">user_id_modified=#{updateDO.userIdModified},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_modified in
                       <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInUserIdModified">
                update role set
                        id=#{updateDO.id},
                        role_code=#{updateDO.roleCode},
                        role_name=#{updateDO.roleName},
                        role_desc=#{updateDO.roleDesc},
                        role_type=#{updateDO.roleType},
                        app_id=#{updateDO.appId},
                        is_extend=#{updateDO.isExtend},
                        user_id_create=#{updateDO.userIdCreate},
                        user_id_modified=#{updateDO.userIdModified},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and user_id_modified in
                       <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInUserIdModified">
                update role set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and user_id_modified in
                        <foreach collection="userIdModifiedList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>

</mapper>

